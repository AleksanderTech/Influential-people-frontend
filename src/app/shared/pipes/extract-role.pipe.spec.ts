import { ExtractRolePipe } from './extract-role.pipe';

describe('ExtractRolePipe', () => {
  it('create an instance', () => {
    const pipe = new ExtractRolePipe();
    expect(pipe).toBeTruthy();
  });
});
