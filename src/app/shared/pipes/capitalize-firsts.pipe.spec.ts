import { CapitalizeFirstsPipe } from './capitalize-firsts.pipe';

describe('CapitalizeFirstsPipe', () => {
  it('create an instance', () => {
    const pipe = new CapitalizeFirstsPipe();
    expect(pipe).toBeTruthy();
  });
});
